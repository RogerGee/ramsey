# Ramsey language test program
#  sum3s.ram - Sum all positive multiples of 3 in [0,n]

fun sum3s(in n)
    if n = 0
       toss 0
    endif

    in thing = n mod 3

    if thing = 0
       toss n + sum3s(n-3)
    endif

    toss sum3s(n-thing)
endfun

in n
take n
give sum3s(n)

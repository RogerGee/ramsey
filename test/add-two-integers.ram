# Ramsey test program: add-two-integers.ram
#  build a driver C program to call 'program' with input 'a' and 'b'

fun add(in a, in b) as in
    toss a + b
endfun

fun program(in a,in b)
    in c <- a*2/2
    in d <- b*100/100

    toss add(c,d)
endfun
